<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/background_color">

    <EditText
        android:id="@+id/et_username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Username"
        android:inputType="text"
        android:backgroundTint="@color/input_background_color"
        android:textColor="@color/input_text_color"
        android:textColorHint="@color/input_hint_color"
        android:contentDescription="Username input field" />

    <EditText
        android:id="@+id/et_new_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="New Password"
        android:inputType="textPassword"
        android:backgroundTint="@color/input_background_color"
        android:textColor="@color/input_text_color"
        android:textColorHint="@color/input_hint_color"
        android:contentDescription="New password input field" />

    <EditText
        android:id="@+id/et_confirm_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Confirm Password"
        android:inputType="textPassword"
        android:backgroundTint="@color/input_background_color"
        android:textColor="@color/input_text_color"
        android:textColorHint="@color/input_hint_color"
        android:contentDescription="Confirm password input field" />

    <Spinner
        android:id="@+id/spinner_security_question"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/input_background_color"
        android:contentDescription="Security question dropdown" />

    <EditText
        android:id="@+id/et_security_answer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Security Answer"
        android:inputType="text"
        android:backgroundTint="@color/input_background_color"
        android:textColor="@color/input_text_color"
        android:textColorHint="@color/input_hint_color"
        android:contentDescription="Security answer input field" />

    <Button
        android:id="@+id/btn_reset_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Reset Password"
        android:backgroundTint="@color/button_background_color"
        android:textColor="@color/button_text_color"
        android:contentDescription="Button to reset password" />

</LinearLayout>
